# Build diff — 1.60.1.69876 → 1.60.1.69913

Client data only: `db2/` against `db2/`, with the hotfix overlay excluded. Live tuning is reported separately by `diff_hotfixes.py`, so everything here shipped in the client.

Generated 2026-09-20T16:07:04+00:00

| | |
|---|--:|
| Tables changed | 2 |
| Tables unchanged | 608 |
| Rows added | 1 |
| Rows removed | 0 |
| Rows changed | 2 |
| New tables | 0 |
| Removed tables | 0 |

---

## Encryption

| Status | Before | After | Delta |
|---|--:|--:|--:|
| `EncryptedUnknownKey` | 3,371 | 3,371 | +0 |
| `EncryptedButNot` | 1,664 | 1,664 | +0 |
| **Total** | **5,035** | **5,035** | **+0** |

`EncryptedUnknownKey` is unchanged — no keys leaked between these builds.

---

## Files

| | |
|---|--:|
| Files before | 1,441,771 |
| Files after | 1,441,771 |
| Added | 0 |
| Removed | 0 |
| Retyped | 0 |
| Renamed | 0 |

---

## GameTables

42 -> 42 table(s).

No GameTable changed.

---

## Schema changes

No layouthash or column changes. Field-level diffs below are valid for every table.

---

## Retail contamination

**NOT SCANNED -- this is not a clean result.**

3 row(s) across 2 table(s) were submitted and **no contamination rule was able to read any of them**. The rules are narrow, and two of the three are pinned to a single table by name, so they returned nothing for lack of anything to read -- not because the data looks clean. Treat this as unmeasured.

| Rule | Why it could not run |
|---|---|
| `dangling_map_ref` | reads continentid, instance_id, instanceid, map_id, mapid; table carries none of them |
| `light_absent_map` | scoped to table Light |
| `orphan_removal` | scoped to table Item and to removed rows; no removed rows |

Submitted tables: `Cfg_GameRules`, `Cfg_SuperDistrict`.

---

## Summary

| Table | Added | Removed | Changed | Rows |
|---|--:|--:|--:|---|
| `Cfg_GameRules` | 1 | 0 | 1 | 194 → 195 |
| `Cfg_SuperDistrict` | 0 | 0 | 1 | 6 → 6 |

⭐ = high-signal table, detailed below. ⚠️ = schema changed, field diffs suppressed. 608 table(s) were byte-identical and are omitted.

---

## Other changed tables

2 further table(s) changed. They are listed in the summary above; re-run with `--detail <table>` for row-level output on any of them.

